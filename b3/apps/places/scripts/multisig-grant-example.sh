#!/usr/bin/env bash
# Reference only — uncomment and fill before running.
# Admin EOA must hold roles; RPC must be Base mainnet.
#
# export RPC_URL=https://mainnet.base.org
# export PRIVATE_KEY=0x...
#
# cast send "$COMPLIANCE_REGISTRY" \
#   "grantRole(bytes32,address)" \
#   "$ROLE_HASH" \
#   "$SAFE_ADDRESS" \
#   --rpc-url "$RPC_URL" \
#   --private-key "$PRIVATE_KEY"

echo "See docs/multisig-base-roles.md for Safe migration checklist."
exit 0
