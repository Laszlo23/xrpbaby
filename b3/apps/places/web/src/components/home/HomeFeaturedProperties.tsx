"use client";

import { HomePortfolioSection } from "@/components/portfolio/HomePortfolioSection";

/** Editorial featured grid — Berggasse + three curated assets (IDs 1, 2, 4, 5). */
export function HomeFeaturedProperties() {
  return <HomePortfolioSection />;
}
