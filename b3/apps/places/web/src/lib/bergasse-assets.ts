/**
 * Shared Berggasse 35 hero still — wider façade read so the Servitenviertel tower reads in 16:9 crops.
 * Used by home hero, Culture Land card, and demo property 1.
 */
export const BERGGASSE_HERO_STILL = "/foto-annablau-dsc1187.jpg";

/** Tailwind classes for `next/image` / `<video>` object-cover framing (hero + video poster). */
export const BERGGASSE_HERO_OBJECT_POSITION_CLASS =
  "object-cover object-[center_50%_18%] sm:object-[center_50%_20%]";
