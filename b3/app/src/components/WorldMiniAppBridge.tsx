/**
 * Placeholder for World App–specific post-load hooks beyond {@link MiniKitProvider}.
 * Host initialization runs inside `@worldcoin/minikit-js` MiniKitProvider (`MiniKit.install`).
 */
export function WorldMiniAppBridge() {
  return null;
}
