/**
 * User-facing product name. Technical identifiers (env, legacy contracts) may still use BUILDCHAIN.
 */
export const BRAND_DISPLAY_NAME = "Build Culture";

/** Short line for hero / meta where space is tight */
export const BRAND_TAGLINE_VAULT = "Real assets. Provably fair. Onchain.";
