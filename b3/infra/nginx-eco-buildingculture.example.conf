# Reference — static SPA for eco.buildingcultureid.space (Vite build → try_files).
# Deploy dist to e.g. /var/www/eco-buildingculture and point root there.

server {
    listen 443 ssl http2;
    listen [::]:443 ssl http2;
    server_name eco.buildingcultureid.space;

    # ssl_certificate /etc/letsencrypt/live/eco.buildingcultureid.space/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/eco.buildingcultureid.space/privkey.pem;

    root /var/www/eco-buildingculture;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff2?)$ {
        expires 7d;
        add_header Cache-Control "public, immutable";
    }
}
