# Optional: serve the same community guide path on the apex domain as on home.
# Merge into your existing server { } for buildingcultureid.space (adjust TLS paths).
#
# Same markdown ships at /guide on umbrella (home), ecorwa (eco), and frontend (0x / app).

location = /guide {
    return 302 https://home.buildingcultureid.space/guide;
}
