# Contract audit findings register

Track Slither, fuzz, and external auditor findings. High severity must be resolved or accepted with sign-off before `ECON_LIVE=1`.

| ID | Contract | Severity | Status | Notes |
|----|----------|----------|--------|-------|
| — | — | — | — | Run `npm run slither` to populate |

Refresh after `bash scripts/run-slither.sh` and external audit delivery.
