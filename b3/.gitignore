node_modules/
dist/
*.log
.DS_Store

# Secrets — never commit (use .env.example templates only)
.env
.env.*
!.env.example
**/.env
**/.env.*
!**/.env.example

# Docker env bake-in (never commit secrets here)
frontend/docker/dotenv-for-build

# Agent wallet material (generate with scripts/generate-agent-wallets.mjs --write-secrets)
.secrets/

# Local IDE / caches (never commit)
.cursor/
**/.metro-cache/
**/.venv/
**/__pycache__/
*.pyc

# Test and build artifacts
test-results/
**/test-results/
contracts/out/
apps/**/out/
apps/**/cache/
**/dist/
proof-bundles/
assets/places-masters/
reports/cleanup-inventory.txt

# Secret-bearing deploy env (use .env.example templates)
apps/**/deploy/.env.production
apps/**/deploy/.env
