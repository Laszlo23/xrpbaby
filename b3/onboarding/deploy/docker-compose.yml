services:
  mongo:
    image: mongo:7
    container_name: buildingculture-landing-mongo
    restart: unless-stopped
    ports:
      - "127.0.0.1:27017:27017"
    volumes:
      - buildingculture_landing_mongo:/data/db

volumes:
  buildingculture_landing_mongo:
